---
name: beast
description: "Alias for beast-forge. Use /beast-forge for the full pipeline."
---

# Beast (alias)

This skill has been superseded by **beast-forge**. Redirecting.

When invoked as `/beast`:
- `beast plan "task"` → invoke `beast-forge` skill with the task
- `beast execute` → invoke `beast-forge --execute`
- `beast status` → check `.omc/plans/` and `.beast-plan/sessions/` for active plans
- `beast "task"` → invoke `beast-forge` skill with the task
