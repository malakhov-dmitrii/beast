# CLAUDE.md Additions

Copy the sections below into your project's CLAUDE.md. Fill them in as you work.

---

## Common Failures (update every ~10 tasks)

- [Add patterns as you discover them]

<!--
Format: one line per pattern. Lead with the symptom, not the fix.
Bad:  "Always use sql.json() for jsonb updates"
Good: "jsonb || merge wraps object in array — use sql.json() to write back"

Only add entries that caused a REAL bug. Good practice doesn't qualify.
Remove entries once a guard is in place that makes the mistake impossible.
-->

---

## Project Docs

Architecture, ADRs, specs, runbooks → docs/INDEX.md. Read before planning.
